<UserControl x:Class="MahTweets.Core.Media.Attachments.ImageMinimisedAttachmentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300">
  <Border BorderThickness="0" BorderBrush="LightGray">
    <StackPanel>
      <Button HorizontalAlignment="Left" VerticalAlignment="Bottom" Style="{DynamicResource ButtonClearedStyle}"
              Click="showhide" Padding="0,0,0,0" Width="35" Height="20">
        <TextBlock x:Name="imgIconHelper" HorizontalAlignment="Left" VerticalAlignment="Bottom"
                   FontFamily="{DynamicResource WebSymbols}" Foreground="{DynamicResource BlackColour}"
                   FontSize="{DynamicResource DefaultFontSize}" Text="I" Opacity="1" Margin="0,0,0,0" />
      </Button>
      <Image Margin="0,0,0,0" x:Name="imageContainer" Source="{Binding Source}" Width="0" Height="0" />
    </StackPanel>
  </Border>
</UserControl>